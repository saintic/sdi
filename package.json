{
    "name": "sdi",
    "version": "1.1.0",
    "description": "Homepage",
    "source": "index.html",
    "author": "Hiroshi.tao <me@tcw.im>",
    "private": true,
    "scripts": {
        "start": "parcel serve --cache-dir /tmp/parcel-cache --dist-dir /tmp/parcel-dist",
        "prebuild": "rm -rf dist",
        "build": "parcel build --cache-dir /tmp/parcel-cache --dist-dir dist"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "2.11.0",
        "@parcel/transformer-jsonld": "2.11.0",
        "parcel": "^2.3.1"
    }
}